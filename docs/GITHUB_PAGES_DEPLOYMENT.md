# GitHub Pages Deployment

Export static files:

```bash
python scripts/export_for_github_pages.py
```

Copy `public/scout/` into `ruslanmv.github.io/scout/` or configure the `deploy_pages.yml` workflow.
